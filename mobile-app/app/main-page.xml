<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo" class="page">
    <Page.actionBar>
        <ActionBar title="AEF Curitiba" icon="" class="action-bar">
        </ActionBar>
    </Page.actionBar>
    <StackLayout class="p-20">
        <Label text="Clique no botão" class="h1 text-center"/>
        <Button text="TAP" tap="{{ clique }}" class="btn btn-primary btn-active"/>
        <Label text="{{ mensagem }}" class="h2 text-center" textWrap="true"/>
        <TextField text="{{ texto }}" class="h2 text-center" textWrap="true"/>
        <Button text="Log" tap="{{ logText }}" class="btn btn-primary btn-active" />
        <Label text="{{ texto }}" class="h2 text-center" textWrap="true"/>
    </StackLayout>
</Page>
